/vendor/
composer.lock
.phpunit.result.cache
.phpunit.cache/

# Local secrets
.env
.env.*
!.env.example

# plugin-check staging area
/plugin-check/dist/

# Scratch. The wp.org listing screenshots live in SVN /assets, never in trunk.
/tmp/
